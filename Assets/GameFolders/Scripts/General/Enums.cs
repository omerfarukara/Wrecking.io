
using UnityEngine;

public enum GameState
{
    Idle,
    Play,
    Finish
}

public enum MotionType
{
    Move,
    LocalMove,
    Scale,
    Jump
}

public enum ShakeType
{
    Position,
    Rotation,
    Scale
}